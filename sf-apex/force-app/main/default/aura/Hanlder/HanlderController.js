({
	myAction : function(component, event, helper) {},
    handleClick:function(component,event,helper){
       
        
        helper.upload_data(component, event, helper);
       // helper.ToastMethod("Value changed","New Value add successfully");
    }
})